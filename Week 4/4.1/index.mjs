import chalk from 'chalk'; // modern import syntax 

// can read more about chalk on the npm registery
// npm registery is a common database for all ecxternal libraries and dependencies and are available to be imported using npm install 
console.log(chalk.blue("Hello"));
console.log(chalk.red.bold("Error message"));
console.log(chalk.green.underline("Success message"));
